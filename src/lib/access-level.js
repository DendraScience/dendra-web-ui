export const accessLevelItems = ['restricted', 'metadata', 'graph', 'download']

export const accessLevelTexts = {
  member: [
    'Members are restricted from viewing metadata and data',
    'Members can view metadata only',
    'Members can view metadata and graph data (no downloading)',
    'Members can view metadata and graph/download datapoints'
  ],
  public: [
    'Public is restricted from viewing metadata and data',
    'Public can view metadata only',
    'Public can view metadata and graph data (no downloading)',
    'Public can view metadata and graph/download datapoints'
  ]
}
